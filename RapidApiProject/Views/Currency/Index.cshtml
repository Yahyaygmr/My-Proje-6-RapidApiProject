@model RapidApiProject.Models.PostCurrencyViewModel
@{
    ViewData["Title"] = "Index";
}

<form method="post" asp-action="ExchangeRates">
    <div asp-validation-summary="All"></div>

   @*  <div class="form-group mt-3">
        <label>Currency</label>
        @Html.DropDownListFor(x => x.CurrencyCode, (List<SelectListItem>)ViewBag.currecies, new { @class = "form-control" })
    </div> *@
    <div class="form-group mt-3">
        <label>Currency</label>
        <select asp-for="CurrencyCode" class="form-control">
            <option value="">Select Code</option>
            @foreach (var item in ViewBag.currecies)
            {
                <option value="@item.Value">@item.Text</option>
            }
        </select>
    </div>
    <div class="form-group mt-3"><button>GetExChangeRates</button></div>

   

</form>

